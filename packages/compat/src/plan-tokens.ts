import { randomUUID } from "node:crypto";
import { DomainError } from "./domain";
import { normalizeProjectPath, pathMatchesPrefix } from "./path-policy";
export const PLAN_TOKEN_TTL_MS = 900000;
type Plan = { projectId: string; scope: "paths" | "project"; writes: Set<string>; deletes: Set<string>; expiresAtMs: number };
export class PlanTokenManager { private readonly plans = new Map<string, Plan>(); issue(projectId: string, scope: "paths" | "project", writes: string[] = [], deletes: string[] = []) { const w = [...new Set(writes.map(normalizeProjectPath))]; const d = [...new Set(deletes.map(normalizeProjectPath))]; if (scope === "paths" && !w.length && !d.length) throw new DomainError("finalize_plan requires writes or deletes for path-scoped plans.", 400, "empty_plan"); const token = `design-plan-${randomUUID()}`; const expiresAtMs = Date.now() + PLAN_TOKEN_TTL_MS; this.plans.set(token, { projectId, scope, writes: new Set(w), deletes: new Set(d), expiresAtMs }); return { token, plan_token: token, expiresAt: new Date(expiresAtMs).toISOString(), expires_at: new Date(expiresAtMs).toISOString(), writes: w, deletes: d, scope }; } validate(token: string | undefined, projectId: string, targets: string[], kind: "writes" | "deletes") { this.prune(); if (!token) throw new DomainError(`${kind === "deletes" ? "delete_files" : "copy_files"} requires plan_token from finalize_plan.`, 400, "plan_required"); const plan = this.plans.get(token); if (!plan) throw new DomainError("plan_token is invalid or expired.", 400, "plan_expired"); if (plan.projectId !== projectId) throw new DomainError("plan_token was issued for a different project.", 403, "plan_project_mismatch"); if (plan.scope === "project") return; const allowed = kind === "deletes" ? plan.deletes : plan.writes; for (const target of targets) if (![...allowed].some((prefix) => pathMatchesPrefix(target, prefix))) throw new DomainError(`plan_token does not cover ${target}.`, 403, "plan_scope"); } prune(now = Date.now()) { for (const [token, plan] of this.plans) if (plan.expiresAtMs <= now) this.plans.delete(token); } }
