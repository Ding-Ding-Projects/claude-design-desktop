# Two-profile manual integration

Status: NOT RUN.

This marker reserves the manual verification that must be performed with two independently authenticated profiles using the packaged Codex 0.152.1 win32-x64 executable. The check must confirm separate `CODEX_HOME` keyring entries, account switching without cross-account state, browser login, device-code login, logout, and renderer-boundary omission of credential material.

No credentials, authorization URLs, or account data belong in this file. This marker is evidence that the manual integration remains unverified, not a claim that it passed.
