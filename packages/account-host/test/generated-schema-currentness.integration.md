# Generated schema currentness integration

Status: NOT RUN.

The packaged runtime integration must generate the app-server TypeScript and JSON Schema outputs from the exact bundled `@openai/codex` 0.152.1 win32-x64 executable, then verify the committed schema-adapter metadata and runtime digest. This marker remains unverified until that packaged-runtime check is run against the installer payload.
